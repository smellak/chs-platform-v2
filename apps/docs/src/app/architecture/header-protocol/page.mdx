# Header Protocol

The CHS header protocol is the mechanism by which the platform communicates user identity and permissions to downstream applications. When a user accesses a registered app through the platform, the platform validates the user's session and injects a set of `x-chs-*` HTTP headers into the forwarded request.

## Header Specification

| Header | Type | Description | Example |
|--------|------|-------------|---------|
| `x-chs-user-id` | UUID | Unique user identifier | `a1b2c3d4-...` |
| `x-chs-user-name` | String | User's display name | `Carlos Martinez` |
| `x-chs-user-email` | String | User's email address | `carlos@centrohogar.es` |
| `x-chs-org-id` | UUID | Organization identifier | `e5f6a7b8-...` |
| `x-chs-org-name` | String | Organization name | `Centro Hogar Sanchez` |
| `x-chs-dept` | String | Department name | `Logistica y Almacen` |
| `x-chs-dept-id` | UUID | Department identifier | `c9d0e1f2-...` |
| `x-chs-role` | Enum | User's role slug | `super-admin`, `dept-admin`, `user`, `viewer` |
| `x-chs-access-level` | Enum | Access level for the current app | `full` or `readonly` |
| `x-chs-permissions` | JSON | JSON-encoded permission map | `{"apps.read":true,"apps.manage":true}` |

## Parsing Headers

The `@chs-platform/sdk` provides the `parseCHSHeaders` function that converts raw headers into a typed `CHSUser` object:

```typescript
import { parseCHSHeaders } from "@chs-platform/sdk";

const user = parseCHSHeaders(request.headers);

if (user) {
  console.log(user.id);          // "a1b2c3d4-..."
  console.log(user.name);        // "Carlos Martinez"
  console.log(user.email);       // "carlos@centrohogar.es"
  console.log(user.orgId);       // "e5f6a7b8-..."
  console.log(user.orgName);     // "Centro Hogar Sanchez"
  console.log(user.dept);        // "Logistica y Almacen"
  console.log(user.deptId);      // "c9d0e1f2-..."
  console.log(user.role);        // "user"
  console.log(user.accessLevel); // "full"
  console.log(user.permissions); // { "apps.read": true, "apps.use": true }
}
```

## CHSUser Interface

The SDK exports the `CHSUser` type that represents a parsed user identity:

```typescript
interface CHSUser {
  id: string;
  name: string;
  email: string;
  orgId: string;
  orgName: string;
  dept: string;
  deptId: string;
  role: "super-admin" | "dept-admin" | "user" | "viewer";
  accessLevel: "full" | "readonly";
  permissions: Record<string, boolean>;
}
```

## CHSHeaders Interface

The raw header names are also typed:

```typescript
interface CHSHeaders {
  "x-chs-user-id": string;
  "x-chs-user-name": string;
  "x-chs-user-email": string;
  "x-chs-org-id": string;
  "x-chs-org-name": string;
  "x-chs-dept": string;
  "x-chs-dept-id": string;
  "x-chs-role": string;
  "x-chs-access-level": string;
  "x-chs-permissions": string;
}
```

## Header Parsing Behavior

The `parseCHSHeaders` function handles several edge cases:

- **Case insensitivity**: Headers are matched in both original case and lowercase
- **Array values**: If a header value is an array (as in some Node.js frameworks), the first element is used
- **Missing `x-chs-user-id`**: If the user ID header is absent, the function returns `null`, indicating no authenticated user
- **Missing optional fields**: Other fields default to empty strings; role defaults to `"viewer"`, accessLevel to `"readonly"`
- **Permissions parsing**: The `x-chs-permissions` header is parsed as JSON. If parsing fails, an empty object is returned

## Security Considerations

The header protocol relies on a trust boundary between the platform and downstream applications:

1. **Headers must come from the platform** -- Applications should only trust `x-chs-*` headers that come through the platform's reverse proxy or API gateway. Direct requests from clients should not contain these headers.

2. **Header stripping** -- The platform strips any incoming `x-chs-*` headers from client requests before adding its own, preventing header spoofing.

3. **Internal network** -- In Docker deployments, app instances communicate with the platform over an internal Docker network. External clients cannot set `x-chs-*` headers on requests to internal services.

4. **Validation** -- Applications should always validate the headers using the SDK rather than reading them manually. The SDK handles missing headers gracefully by returning `null`.

## Usage Patterns

### Express Application

```typescript
import { chsMiddleware, requireCHS } from "@chs-platform/sdk/express";

const app = express();

// Parse headers on all routes
app.use(chsMiddleware());

// Require authentication on specific routes
app.get("/api/data", requireCHS(), (req, res) => {
  const user = req.chs; // CHSUser
  res.json({ message: `Hello ${user.name}` });
});
```

### Next.js Application

```typescript
import { getCHSUser } from "@chs-platform/sdk/next";

export async function GET(request: Request) {
  const user = getCHSUser(request);

  if (!user) {
    return Response.json({ error: "Unauthorized" }, { status: 401 });
  }

  return Response.json({ message: `Hello ${user.name}` });
}
```

### Manual Parsing

```typescript
import { parseCHSHeaders } from "@chs-platform/sdk";

// Works with any header-like object
const headers = {
  "x-chs-user-id": "abc-123",
  "x-chs-user-name": "Test User",
  "x-chs-user-email": "test@example.com",
  "x-chs-org-id": "org-456",
  "x-chs-org-name": "Test Org",
  "x-chs-dept": "Engineering",
  "x-chs-dept-id": "dept-789",
  "x-chs-role": "user",
  "x-chs-access-level": "full",
  "x-chs-permissions": '{"apps.read":true}',
};

const user = parseCHSHeaders(headers);
```
